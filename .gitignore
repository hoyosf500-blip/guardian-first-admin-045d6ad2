# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code / agentes locales
.claude/
everything-claude-code/
.playwright-mcp/

# Backups locales
*.bak

# Supabase CLI temp
supabase/.temp/

# Capturas de pantalla (solo raíz)
/*.png

# Script de prueba sandbox Dropi (tiene credenciales — NO subir)
scripts/dropi-sandbox-test.mjs

# Volcados del DOM de pantallas del CRM: /admin muestra los inputs de
# credenciales con su VALOR, así que un snapshot de esa página lleva dentro la
# integration-key permanente de Dropi. Pasó: page-admin-snapshot.yml estuvo en
# origin/main desde b93ddc5 (18-jul) con la clave de Ecuador en claro.
*-snapshot.yml
*-snapshot.yaml

# Tokens de API sacados de un panel (Chatea Pro, etc.). NUNCA van al repo:
# el 11-ago-2026 una integration-key de Dropi terminó publicada en `main`.
chateapro-token-*.txt
*.key

# ⛔ Capturas de pantalla/API que se hacen para diagnosticar (3-sep-2026).
# NUNCA van al repo: `imporchat-convo1.json` traia 22 telefonos y nombres de
# clientes REALES de Ecuador, y entro por un `git add -A` distraido. Son
# volcados de la UI de terceros para mirar un bug; se leen y se tiran.
chat-center-estado.yml
dropi-nav.json
dropi-orders-setting.json
dropi-shop.json
imporchat-convo*.json
imporchat-nav.json
imporchat-novedad-acciones.json
imporchat-seg-*.json
subs*.json
